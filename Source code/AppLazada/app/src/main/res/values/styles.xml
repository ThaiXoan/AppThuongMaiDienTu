<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/background_toolbar</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/yellow_900</item>
    </style>

    <style name="customTitleOfTablayout" parent="TextAppearance.Design.Tab">
        <item name="textAllCaps">false</item>
    </style>
    
    <style name="menuItemColor" parent="AppTheme">
        <item name="android:textColorPrimary">@color/black</item>
        <item name="drawerArrowStyle">@style/chageColorDrawertoogle</item>
    </style>

    <style name="menuItemColorWhite" parent="AppTheme">
        <item name="android:textColorPrimary">@color/white_color</item>
    </style>

    <style name="chageColorDrawertoogle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="color">@color/white_color</item>
    </style>
    
    <declare-styleable name="passwordEditText">
        <attr name="useHint" format="boolean"/>
        <attr name="useValidate" format="boolean"/>
    </declare-styleable>
</resources>
